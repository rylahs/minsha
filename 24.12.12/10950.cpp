#include <bits/stdc++.h>
using namespace std;

int main(void)
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	
	int t = 0;
	cin >> t;

	while (t--)
	{
		int a, b;
		cin >> a >> b;

		cout << a + b << '\n';
	}
	return 0;
}